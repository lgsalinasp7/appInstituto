name: Cron - Kaled Daily

on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger kaled-daily
        run: |
          resp=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ vars.VERCEL_CRON_URL }}/api/cron/kaled-daily")
          [ "$resp" = "200" ] || (echo "Failed: $resp" && exit 1)
