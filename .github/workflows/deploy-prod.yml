name: Deploy producción

on:
  push:
    branches:
      - main

jobs:
  migrate-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Run migrations en producción
        run: npx prisma migrate deploy
        env:
          # Schema requiere DATABASE_URL; migrate usa DIRECT_URL (evita P1002 advisory lock)
          DATABASE_URL: ${{ secrets.DIRECT_DATABASE_URL_PROD }}
          DIRECT_URL: ${{ secrets.DIRECT_DATABASE_URL_PROD }}
          MIGRATE_ADVISORY_LOCK_TIMEOUT: "30000"

      - name: Ensure Kaled tables (producción)
        run: npx tsx prisma/ensure-kaled-tables.ts
        env:
          DATABASE_URL: ${{ secrets.DIRECT_DATABASE_URL_PROD }}

      - name: Trigger deploy en Vercel prod
        run: |
          resp=$(curl -s -o /dev/null -w "%{http_code}" -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_PROD }}")
          [ "$resp" = "200" ] || [ "$resp" = "201" ] || (echo "Deploy hook failed: HTTP $resp" && exit 1)
