/**
 * Next.js client-side instrumentation hook (Next 15.3+ / 16.x).
 *
 * Carga la config de Sentry para el browser. Es el equivalente client
 * del archivo `instrumentation.ts` (server/edge).
 */
import "./sentry.client.config";
